# 云笔记 AI 助手项目接管说明

> 更新时间：2026-09-18  
> 交接对象：Claude 或其他后续开发代理  
> 目标：在现有实现上继续开发，禁止从头重做。

## 1. 项目位置

- 项目根目录：`D:\JAVA-Study\project\notepad`
- 后端：`D:\JAVA-Study\project\notepad\backend`
- 前端：`D:\JAVA-Study\project\notepad\notepadweb`
- 数据库脚本：`D:\JAVA-Study\project\notepad\sql`
- 设计与部署文档：`D:\JAVA-Study\project\notepad\docs`

后续代理开始工作前，必须先读取本文件、根目录 `README.md`，以及 `docs/01-需求分析.md`、`docs/02-数据库设计.md`、`docs/03-接口设计.md`、`docs/05-部署上线指南.md`，再检查实际代码。不要根据本文件臆造不存在的接口或功能。

## 2. 产品目标

这是一个多用户云笔记系统，核心目标是：

1. 用户可以安全地管理笔记、笔记本、标签、图片、回收站、日历和提醒。
2. 用户可以向 AI 助手提问，AI 只依据自己的笔记回答，并展示可追溯引用。
3. RAG 检索范围必须严格支持全部笔记、指定笔记本、当前笔记三种模式，不能跨用户或跨笔记本引用。
4. AI 回答要支持流式显示、多轮会话、历史恢复、反馈和回归评估。
5. 后续 AI 编辑能力必须采用“生成预览 → 用户确认 → 写回笔记”，禁止模型直接覆盖用户内容。

## 3. 技术栈和运行方式

### 后端

- Java 17
- Spring Boot 3.3.7
- Spring AI 1.0.3
- Spring AI Alibaba DashScope 1.0.0.3
- 阿里云百炼 Chat：`qwen-plus`
- 阿里云百炼 Embedding：`text-embedding-v4`，维度 1024
- MySQL 8
- MyBatis-Plus 3.5.7
- JWT（jjwt 0.12.6）
- 当前向量库：持久化 `SimpleVectorStore`
- 默认后端端口：8080

启动后端：

```powershell
cd D:\JAVA-Study\project\notepad\backend
mvn spring-boot:run
```

### 前端

- Vue 3
- TypeScript
- Vite
- Pinia
- Element Plus
- WangEditor
- 默认前端端口：5173

启动前端：

```powershell
cd D:\JAVA-Study\project\notepad\notepadweb
npm install
npm run dev
```

## 4. 已完成并验证的功能

### 4.1 普通云笔记功能

- 注册、登录、JWT 鉴权、用户隔离
- 笔记本、笔记、标签的增删改查
- 富文本编辑、自动保存、本地草稿恢复
- 图片上传和访问
- 全文搜索、分页、置顶
- 回收站、恢复、彻底删除、定时清理
- 日历视图、提醒、站内通知

### 4.2 RAG 索引和检索

- 数据库真实笔记转换为 Spring AI `Document`
- 自动切片、Embedding、持久化到 `SimpleVectorStore`
- 向量数据按用户隔离
- 笔记新增、编辑、移动、删除时自动同步索引
- 索引健康检查和一键重建
- 已验证 Embedding 维度为 1024
- 相关文本相似度约 0.8645，无关文本约 0.1258

### 4.3 AI 流式助手

- `POST /api/ai/notes/ask/stream` 使用 SSE 流式回答
- 前端逐字显示回答
- 回答严格依据检索到的笔记
- 回答结束显示引用片段、相似度和原文
- 点击引用可跳转原笔记并定位、高亮
- 支持停止生成、超时、重试、错误编号
- 支持全部笔记、当前笔记本、当前笔记范围
- 后端会按数据库实时归属再次校验检索结果，防止跨笔记本引用

### 4.4 多轮会话和反馈

- 连续追问
- 多个历史会话
- 会话、消息和引用保存到 MySQL
- 切换浏览器或重新登录后可恢复
- 用户之间会话隔离
- 每条完整回答支持“有帮助 / 没帮助”
- 没帮助可填写原因和补充说明
- 支持修改反馈
- 反馈保存问题、回答、引用、检索范围和会话消息
- 支持反馈原因统计和常见失败问题统计
- 支持把“没帮助”回答快速转换为回归测试用例

### 4.5 RAG 回归评估

已执行数据库升级脚本：

- `006_ai_conversation.sql`
- `007_ai_answer_feedback.sql`
- `008_ai_rag_evaluation.sql`
- `009_ai_rag_eval_match_mode.sql`
- `010_ai_feedback_eval_link.sql`

回归测试支持：

- 新增、编辑、启停、删除测试用例
- 全部笔记、指定笔记本、当前笔记范围
- “应命中”和“应拒答”
- 允许命中的预期笔记集合
- 最低相关度
- 批量运行和历史报告
- 整体通过率、Top-K 命中率、正确拒答率、错误引用数、平均检索耗时
- 每个失败用例的详细原因
- 回归只调用 Embedding 检索，不调用回答模型

### 4.6 日志、追踪和当前稳定性能力

最近已经完成：

- `X-Request-Id` 请求编号和 MDC 日志关联
- HTTP 请求耗时日志
- AI 检索耗时日志
- AI 首 token、生成耗时、总耗时日志
- RAG 回归运行完成日志
- SSE 成功、超时、连接断开、异常日志
- 流式、阻塞问答、普通聊天统一并发限制
- 全局 AI 并发上限默认 4
- 单用户 AI 并发上限默认 1
- 并发不足时返回可重试提示和 `Retry-After: 2`
- AI 流式线程池队列从 100 调整为 16

相关文件：

- `backend/src/main/java/com/notepad/common/RequestTraceContext.java`
- `backend/src/main/java/com/notepad/config/RequestTraceFilter.java`
- `backend/src/main/java/com/notepad/ai/service/AiConcurrencyLimiter.java`
- `backend/src/main/java/com/notepad/ai/controller/AiStreamController.java`
- `backend/src/main/java/com/notepad/ai/controller/AiController.java`
- `backend/src/main/java/com/notepad/ai/config/AiConfiguration.java`
- `backend/src/main/resources/application.yml`

## 5. 当前已知基线和验证结果

### RAG 基线

当前固定回归基线共 14 个用例，最近一次已验证：

- 整体通过率：100%
- Top-K 命中率：100%
- 正确拒答率：100%
- 错误引用：0
- 报告时间：2026-09-18 14:58:56

SQL 优化用例允许命中 day10 和 day12；不要把确实相关的额外笔记误判为 RAG 错误，也不要盲目调低或调高阈值。

### 最近的代码验证

- 后端受影响源码已用 Java 17 编译通过
- Maven 编译已通过：`mvn -DskipTests -Dmaven.resources.skip=true compile`
- 前端 TypeScript 检查已通过：`npx vue-tsc --noEmit`
- 前端生产构建已通过（使用独立临时输出目录）
- 后端已重启并成功监听 8080
- 未登录请求仍返回 401，且保留 `X-Request-Id`
- 并发限制器独立验证通过：同用户第二个请求拒绝、全局满载拒绝、释放后恢复

Windows 注意事项：

- Maven 第一次可能出现无法关闭编译器资源，再次执行通常会成功。
- 当前环境有时会锁定 `target`、Maven 本地仓库或 Vite 缓存目录；不要删除用户数据或清空项目。可以使用独立临时输出目录验证构建。
- 不要打印或提交数据库密码、SMTP 授权码、JWT 密钥、百炼 API Key。环境变量来源于本机 IDE 配置或运行环境。

## 6. 现在做到哪一步

项目已经完成核心产品、RAG、流式助手、会话、反馈、回归基线、请求追踪、基础并发降级、索引备份恢复、限流与耗时指标、反馈与回归报告的双向追溯，以及 pgvector 接入与迁移对比。

### 6.3 部署就绪性（2026-09-19 验收通过）

- **密钥全部走环境变量注入**，代码里没有明文密钥。必填项见 `docs/05-部署上线指南.md` 第 2.2 节（**六个必填**：`AI_DASHSCOPE_API_KEY` / `MAIL_USERNAME` / `MAIL_PASSWORD` / `DB_USERNAME` / `DB_PASSWORD` / `JWT_SECRET`）
- **后端 jar 可直接运行**：`mvn -o -DskipTests package` 产出 `backend/target/notepad-backend-0.0.1-SNAPSHOT.jar`（57 MB fat jar），`java -jar` 带上述环境变量即可启动，实测 5.5 秒起来并正常服务
- **`upload.dir` 已改为可配置**：`NOTEPAD_UPLOAD_DIR`，相对路径按**后端模块**解析（与向量库文件同一套规则），不再硬编码 `/root/uploads`
- **本机 Nginx 全链路验收通过**：Nginx 托管前端 `dist` 并反代 `/api` 与 `/uploads`，SPA 子路由回退、嵌套路径、写操作、大响应体、**SSE 流式（AI 问答与 AI 编辑预览）**全部正常，error 日志 0 条

> **Nginx 配置里有一条部署文档原先没写的关键项**：SSE 接口所在的 `location` 必须 `proxy_buffering off`，否则 Nginx 会攒够一整块才转发，前端就不再逐字显示。已补进 `docs/05-部署上线指南.md` 与实测配置。

### 6.4 当前未完成

只剩**真正上云部署**（方案 B）——需要采购云服务器，属于运维动作而非代码工作。代码侧的部署就绪性已验收完毕。

### 6.1 AI 编辑能力（2026-09-19 完成）

四种能力：**摘要 / 改写 / 续写 / 提取待办**。流程严格遵循「读取当前笔记 → AI 生成预览 → 用户确认 → 写回笔记 → 重新同步向量索引」：

- 预览走 SSE 流式，**接口不写库**；写回必须显式调用 apply 接口
- 写回前用 **正文 SHA-256 内容指纹**挡竞态：预览期间笔记被改过就返回 409，绝不覆盖
- 写回前自动把原文存入 `ai_note_revision`（`011_ai_note_revision.sql`），每篇笔记保留最近 10 份，可一键恢复原文
- AI 输出**先做 HTML 转义再包 `<p>`** —— 正文由富文本编辑器渲染，不转义会把模型输出里的标签变成可执行标记
- 写回复用 `NoteService.update`，因此**必须带上笔记当前的 title 与 tagIds**：该方法是全量覆盖，`saveTags` 会先删光再重建，不带就是一次编辑清空用户标签（验收脚本里有专项断言）

### 6.2 索引存储现状

- 默认向量存储仍是进程内的 `SimpleVectorStore`，数据文件 `backend/data/simple-vector-store.json`
- pgvector 已完整接入，`NOTEPAD_VECTOR_STORE_TYPE=pgvector` 一个环境变量即可切换，两种存储下的接口、快照格式、备份恢复流程完全一致
- 切换与迁移方式：**在一种模式下备份 → 切到另一种模式 → 恢复同一份快照**，不需要专门的迁移工具
- 相关文档：[07-pgvector迁移对比报告](./docs/07-pgvector迁移对比报告.md)

## 7. Claude 接下来必须按此顺序执行

### 第一阶段：索引备份与恢复

这是当前最近的任务，不要先做 pgvector，也不要先开发 AI 写回。

要求：

1. 先阅读 `NoteVectorService`、索引状态/重建接口、`application.yml` 和现有向量文件保存逻辑。
2. 设计增量、可恢复的索引备份方案，不清空现有 `simple-vector-store.json`。
3. 备份写入必须尽量采用临时文件 + 原子替换，避免进程中断留下半个 JSON。
4. 恢复前校验文件存在、JSON 可解析、向量维度正确、用户数据边界正确。
5. 恢复失败不能破坏当前可用索引。
6. 增加健康检查、备份时间、文件大小、恢复结果和错误编号日志。
7. 如需数据库字段或表，必须新增 `011_*.sql` 增量脚本，不能修改或重跑已有脚本。
8. 增加最小可验证的后端测试或脚本，并在 UI/接口层验证一次备份和恢复。

阶段完成标准：

- 能创建带时间戳的索引备份
- 能从备份恢复并重新通过健康检查
- 模拟损坏备份时，原索引仍然可用
- 14 个固定 RAG 用例仍然保持 100% 或每个差异都有人工确认
- 后端编译、前端类型检查、生产构建通过

### 第二阶段：稳定性和反馈闭环完善

- 统计限流拒绝数、超时数、重试数、错误编号和耗时分布
- 检查并发限制释放是否覆盖正常完成、客户端断开、超时、异常和服务重启
- 将反馈失败问题与回归测试报告做可追踪关联
- 不要因为单个失败用例盲目修改检索阈值，先判断预期集合是否完整

### 第三阶段：迁移到 pgvector

只有第一、第二阶段稳定后才开始。

- 保留 SimpleVectorStore 作为可回退方案
- 设计用户隔离、笔记范围过滤、向量维度和索引结构
- 用同一批 14 个以上固定回归用例对比迁移前后
- 比较 Top-K 命中率、正确拒答率、错误引用数和平均检索耗时
- 对比结果未确认前，不删除现有向量文件

### 第四阶段：AI 编辑能力

支持：摘要、改写、续写、提取待办。所有能力必须遵守：

```text
读取当前笔记 → AI 生成预览 → 用户确认 → 写回笔记 → 重新同步向量索引
```

模型不得直接覆盖笔记；写回失败时不能丢失原文。

## 8. 最终项目完成判定

不能只因为“能启动”就宣布项目完成。最终完成必须同时满足：

- 核心笔记功能可用，用户数据隔离通过
- AI 流式问答、引用跳转、多轮会话、反馈均可用
- 固定 RAG 回归基线稳定，错误引用为 0 或有人工确认记录
- 索引备份和恢复经过真实验证
- 并发限制、超时、重试、降级、请求追踪有日志证据
- pgvector 迁移前后有对比报告，或明确记录暂不迁移的原因
- AI 编辑写回必须经过预览确认
- 后端编译通过、前端 TypeScript 检查通过、生产构建通过
- 数据库变更均有增量 SQL，现有数据没有被清空
- 生产配置中的密钥全部通过环境变量注入

## 9. 给 Claude 的直接指令

```text
你正在接管一个已经完成核心功能的云笔记 AI 助手项目。
请先读取 CLAUDE_HANDOFF.md、README.md 和 docs 下的设计文档，再检查实际代码。
不要从头重做，不要清空数据库，不要删除 SimpleVectorStore，不要盲目调整 RAG 阈值。

当前最近任务是“索引备份与恢复验证”，不是 pgvector 迁移，也不是重新开发 RAG。
请一次只完成一个小阶段：先检查现有索引保存逻辑，提出最小改动方案，修改后执行后端编译、前端类型检查和生产构建，并报告验证结果。
如果需要数据库变更，只能增加新的 011_* 增量脚本。
任何写回笔记的 AI 编辑功能都必须先返回预览并等待用户确认。
完成一个阶段后，说明：改了哪些文件、保留了哪些既有功能、验证了什么、下一步是什么。
```

## 10. 完成时间口径

本项目不以“启动成功”或“代码写完”作为完成时间，而以第 8 节的验收条件为准。

- 已完成的里程碑：
  - 核心产品 + RAG 基线 + 会话/反馈 + 请求追踪 + 基础并发降级（2026-09-18）
  - 索引备份与恢复验证（2026-09-19）：备份创建、恢复、损坏快照保护、路径穿越防护、14 个固定用例复测全部通过
  - 稳定性与反馈闭环（2026-09-19）：并发许可释放实测（守恒律 14 项）、限流/超时/重试/耗时指标（18 项）、反馈与回归报告双向追溯（18 项）
  - pgvector 接入与迁移对比（2026-09-19）：迁移逐位无损、三种配置逐用例引用完全一致、对比报告已出
  - AI 编辑能力（2026-09-19）：摘要 / 改写 / 续写 / 提取待办，预览流式、写回走内容指纹校验、原文快照可回退，21 项验收全过
  - 文档同步与部署就绪性验收（2026-09-19）：接口文档补齐 AI 问答/会话/反馈/回归评估两章（共 17 章）、jar 直接运行验证、本机 Nginx 全链路验收通过
- 当前最近里程碑：**真正上云部署**（需采购服务器，属运维动作）。
- 代码侧第 8 节的验收条件已全部满足；`docs/03-接口设计.md` 不再有缺口。
- pgvector 的处置：**已完成迁移验证，但默认值保持 `simple`**。这是有依据的决定而非未完成 —— 三种配置检索质量完全等价，pgvector 的 `NONE` 档慢 4.5%、`HNSW` 档慢 44%，而 0.8 MB 的数据规模上多跑一个数据库进程没有收益。切换方式与触发条件写在对比报告第 7 节。
- 项目整体完成时间：完成 AI 编辑预览确认和生产验收之后再确定；在这些条件未满足前，不要对用户宣称”整体完成”。
- 如果某个阶段因环境权限、外部 API 或数据库状态无法验证，必须明确标记为”待验证”，不能用推测结果代替完成证据。目前明确标记为未验证的有：`SseEmitter.onTimeout` 路径、AI 线程池队列满路径（详见 `backend/scripts/verify-ai-concurrency.sh` 末尾的「未覆盖项」）。
